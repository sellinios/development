# Environment configuration for site.epsilonhellas.com deployment

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=intranetuser
DB_PASSWORD=secure_password_here
DB_NAME=intranet_epsilon
DB_SSLMODE=disable

# Application Settings
PORT=8080
APP_ENV=production
JWT_SECRET=change_this_to_a_secure_random_string

# Frontend Settings
NEXT_PUBLIC_API_URL=/intranet/api